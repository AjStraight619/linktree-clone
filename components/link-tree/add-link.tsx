import { UserLinkAction } from "@/lib/types";
import React from "react";

type AddLinkProps = {
  dispatch: React.Dispatch<UserLinkAction>;
};

const AddLink = () => {
  return <div>AddLink</div>;
};

export default AddLink;
